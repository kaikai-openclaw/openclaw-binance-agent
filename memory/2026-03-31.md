# 2026-03-31

## Skill1 筛选机制 v2 升级

按优先级完成了 skill1_collect.py 的全面优化：

### P0 — 支持做空信号
- 涨跌幅过滤改为绝对值区间（|change| in [pc_min, pc_max]），同时纳入上涨和下跌标的
- 评分系统改为双向：分别计算做多/做空得分，取高分方向
- 输出新增 `signal_direction: "long" | "short"` 字段

### P0 — 连续化评分函数
- RSI 评分从 4 档阶梯改为线性插值（30→满分, 80→0分）
- 权重重新分配：RSI(30) + EMA(20) + MACD(20) + ADX(15) + 流动性(15) = 100

### P1 — ATR 维度
- 新增 calc_atr() 纯函数（Wilder 平滑）
- 输出 atr（绝对值）和 atr_pct（占收盘价百分比），供 Skill3 止损止盈使用

### P1 — 成交额加权
- 新增流动性评分因子：log(vol)/log(max_vol) 归一化，15 分权重

### P1 — ADX 趋势强度
- 新增 calc_adx() 纯函数
- ADX 0~50 线性映射到 0~15 分，区分趋势/震荡市场

### P2 — 相关性去重
- 新增 calc_correlation() 和 calc_returns()
- Step 4: 按评分排序后逐个加入，与已选候选相关系数 > 0.85 则跳过

### Schema 更新
- skill1_output.json 新增 signal_direction, atr, atr_pct, adx 字段
- skill1_input.json 更新 price_change_range 描述

### 测试
- skill1 单元测试：79 个全部通过（新增 ATR/ADX/相关性/双向评分测试）
- 集成测试：修复 _make_mock_binance_public 使用 noise_seed 避免相关性去重
- 全量测试通过
